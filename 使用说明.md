# CueDesk

内置本地服务的窗口程序：打开任意 HTML 幻灯片，笔记本翻页，扩展屏全屏同步。

## 运行

1. 安装 Node.js
2. 双击 `启动容器.bat`（首次会 `npm install`）
3. 菜单「文件 - 打开 HTML」
4. 点页面上的「开始演示」，或按 F8
5. 改 html 后按 F5 刷新

屏幕必须是「扩展」。

## 幻灯片要求

`.slide` + 当前页 `active`，或 `window.goToSlide(页码)`。

CueDesk 不做可视化编辑，保存仍用你原来的编辑器；CueDesk 负责打开、投屏、刷新。
